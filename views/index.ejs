<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta http-equiv="X-UA-Compatible" content="ie=edge">
  <link href="/css/main.css" rel="stylesheet" type="text/css">
  <title>Short Links</title>
</head>

<body>

  <div class="jumbotron bg-white">
    <div class="container">
      <div class="row">
        <div class="col-md-8">
          <h1 class="display-4 font-weight-bolder">Short links, big results</h1>
          <p class="lead font-weight-normal">A URL shortener built with powerful tools to help you grow and protect your
            brand.</p>
        </div>
        <div class="col-md-4">
          <img src="./img/illo.jpg">
        </div>
      </div>
    </div>
  </div>

  <div class="container-fluid pt-3 pb-3 bg-form">
    <div class="container">
      <form action="/shortUrls" method="POST" class="my-4 form-inline short-form">
        <label for="fullUrl" class="sr-only">Url</label>
        <input required placeholder="Url" type="url" name="fullUrl" id="fullUrl"
          class="form-control form-control-lg col mr-2">

        <label for="slugUrl" class="sr-only">Slug</label>
        <input placeholder="Slug" minlength="4" type="text" name="slugUrl" id="slugUrl"
          class="form-control form-control-lg col mr-2">

        <button class="btn btn-primary btn-lg" type="submit">Shorten</button>
    </div>
    </form>

    <% if (errors.vld) { %>
    <div id="alert" class="alert alert-danger">
      <%= errors.msg %>
    </div>
    <% } %>
  </div>


  <div class="container mt-5">
    <table class="table table-responsive table-striped">
      <thead>
        <tr class="text-center">
          <th>Full URL</th>
          <th>Short URL</th>
          <th>Clicks</th>
          <th>Stats</th>
        </tr>
      </thead>
      <tbody>
        <% shortUrls.forEach(shortUrl => { %>
        <tr class="text-center">
          <td><a href="<%= shortUrl.full %>"><%= shortUrl.full %></a></td>
          <td><a href="<%= shortUrl.short %>"><%= shortUrl.short %></a></td>
          <td><%= shortUrl.clicks %></td>
          <td><a class="btn btn-primary" href="<%= shortUrl.short %>/stats">Stats</a></td>
        </tr>
        <% }) %>
      </tbody>
    </table>
  </div>
</body>

</html>